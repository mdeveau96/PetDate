import { describe, test } from 'vitest'
import { setup, $fetch } from '@nuxt/test-utils/e2e'

describe('Test Homepage', async () => {
    await setup({
        // test 
    })

    test('test homepage', () => {
        // test
    })
})